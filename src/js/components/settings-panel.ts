// Settings panel for the map

import h from '@macrostrat/hyper'

const SettingsPanel = (props)=>{
  return h("div.settings", [
    h("p", "No settings defined for the current view.")
  ])
}

export {SettingsPanel}
