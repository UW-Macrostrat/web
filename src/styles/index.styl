@require "./global.styl"
@require "./searchbar.styl"
@require "./infodrawer.styl"
@require "./expansion-panel.styl"

div.ui
  position relative
  margin 20px
  margin-bottom 0
  // Make sure we can still click
  // on map in background
  pointer-events none
  &>div
    pointer-events all

