:global
  @require "./global.styl"
  @require "./searchbar.styl"
  @require "./infodrawer.styl"
  @require "./ui-components.styl"

  div.ui
    position relative
    margin 0 20px
    margin-bottom 0
    // Make sure we can still click
    // on map in background
    pointer-events none
    &>div
      padding 20px 0
      pointer-events all
