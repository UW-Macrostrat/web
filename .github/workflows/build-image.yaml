name: Build image
permissions: read-all
on:
  push:
    branches: ["main"]
    tags:
      - "v*.*.*" # glob for semver tags (including prereleases)
  pull_request:
    branches: [main]
  # Fired by github.com/Macrostrat/docs when the documentation vault changes,
  # so the /docs section is rebuilt without a commit here.
  repository_dispatch:
    types: [docs-updated]

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          submodules: "recursive"
      # The /docs section of the site is assembled from the documentation vault
      # (github.com/Macrostrat/docs) into ./content before the image is built.
      # The vault is checked out under .cache/ (docker-ignored); only the
      # assembled content/ tree reaches the image.
      - name: Checkout documentation vault
        uses: actions/checkout@v3
        with:
          repository: Macrostrat/docs
          ref: main
          path: .cache/docs
      - name: Assemble documentation
        env:
          DOCS_SOURCE_DIR: .cache/docs
        run: |
          corepack enable
          bash scripts/assemble-docs.sh
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: hub.opensciencegrid.org/macrostrat/web
          # New: apply the 'latest' tag only to non-prerelease semver tags
          tags: |
            type=raw,value=sha-{{sha}}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') && !contains(github.ref, '-')}}
            type=ref,event=pr,suffix=-{{date 'YYYYMMDDHHmmss'}}
            type=ref,event=branch,suffix=-{{date 'YYYYMMDDHHmmss'}}
            type=ref,event=tag,suffix=-{{date 'YYYYMMDDHHmmss'}}
            type=semver,pattern={{version}}
            type=semver,pattern={{version}},suffix=-{{date 'YYYYMMDDHHmmss'}}
            
            # When a PRODUCTION tag (vX.Y.Z) is pushed → also tag staging and dev
            type=raw,value=main-{{date 'YYYYMMDDHHmmss'}},enable=${{ startsWith(github.ref, 'refs/tags/v') && !contains(github.ref, '-') }}
            type=semver,pattern={{version}},suffix=-beta.{{date 'YYYYMMDDHHmmss'}},enable=${{ startsWith(github.ref, 'refs/tags/v') && !contains(github.ref, '-') }}
            type=semver,pattern={{version}},suffix=-beta,enable=${{ startsWith(github.ref, 'refs/tags/v') && !contains(github.ref, '-') }}

            # When a STAGING prerelease tag (vX.Y.Z-*) is pushed → also tag dev
            type=raw,value=main-{{date 'YYYYMMDDHHmmss'}},enable=${{ startsWith(github.ref, 'refs/tags/v') && contains(github.ref, '-') }}
          flavor: |
            latest=false
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to OSG DockerHub
        uses: docker/login-action@v2
        with:
          registry: hub.opensciencegrid.org
          username: ${{ vars.HARBOR_CLI_NAME }}
          password: ${{ secrets.HARBOR_CLI_SECRET }}
      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
