export default {
  pageInfo: { name: "xDD" },
  // The expanded breadcrumb form: page title on its own line, with any item
  // identifier right-aligned. List pages override with `pageStyle: "fullscreen"`
  // and render `InfiniteScrollPage`, which brings its own header and footer.
  pageStyle: "content",
};
