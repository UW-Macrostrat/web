export default {
  pageInfo: { name: "Names" },
  // `InfiniteScrollPage` supplies breadcrumbs and footer itself, and the list
  // is the page — the same full-height scrolling frame the map-ingestion list
  // uses.
  pageStyle: "fullscreen",
};
