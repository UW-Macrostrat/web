export default {
  pageInfo: { name: "Lexicon" },
  // "content" gives the expanded breadcrumbs (title on its own line, with any
  // item identifier right-aligned in the title row). Individual pages that need
  // a different shell (e.g. the fullscreen measurements map) override with their
  // own +config.ts.
  pageStyle: "content",
};
