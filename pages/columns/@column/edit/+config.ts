export default {
  // The editor runs full-width in the hybrid frame — no map, no sidebar (see
  // `../column-editor/editor-shell.ts`). The frame style and client-only
  // rendering are inherited from the column route's `+config.ts`.
  title: "Edit column",
  description:
    "Experimental column editor: units and surfaces edited side by side.",
};
